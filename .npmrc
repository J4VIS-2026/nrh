# 인식 모듈의 optional peer를 자동 설치하지 않는다. `omit=peer`는 루트의 직접 의존성인
# nrh-detector까지 누락할 수 있으므로 사용하지 않는다.
#
# React Native 로 쓸 사람은 **앱 쪽에서** onnxruntime-react-native 를 설치해야 한다 —
# 네이티브 모듈이라 다른 패키지의 node_modules 안에 있으면 자동 링크가 안 된다.
legacy-peer-deps=true
