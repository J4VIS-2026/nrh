# 남의 코드와 라이선스

**이 저장소는 AGPL-3.0-only입니다.** 전체 조문은 같은 폴더의 `LICENSE`에 있습니다.

**법률 자문이 아닙니다.** 사실만 적어둔 것이니, 공개 전에 판단이 필요하면 확인을 받으세요.

---

## 왜 AGPL인가 — 고를 수 있는 것이 아니었습니다

추적기(BoT-SORT)를 Ultralytics에서 가져왔고 **Ultralytics가 AGPL-3.0**입니다. AGPL은
**그 코드를 쓴 전체 저작물도 같은 라이선스로** 내놓게 합니다. 모델도 Ultralytics로
학습했습니다.

**AGPL은 "네트워크로 쓰게 하는 것"도 배포로 봅니다.** 웹 데모를 서버에 올려 남이 쓰게
하면, 쓰는 사람에게 소스를 받을 길을 열어줘야 합니다. 사내에서만 쓰면 해당하지 않습니다.

---

## 가져온 코드

| 무엇 | 어디서 | 라이선스 | 고쳤나 |
|---|---|---|---|
| BoT-SORT 추적기 | `ultralytics` 8.4.114 | **AGPL-3.0** | **안 고침** |
| 모델 가중치 | Ultralytics YOLOv8n으로 학습 | **AGPL-3.0** | 우리가 학습·양자화 |

JS 추적 코드는 Ultralytics 8.4.114의 BoT-SORT 구현을 바탕으로 TypeScript로 포팅했습니다.
원출처와 적용 라이선스는 위 표와 이 문서에 유지합니다.

---

## 고쳐서 쓰는 남의 코드 (React Native 앱)

앱 저장소(`nrh-test-app`)의 `node_modules` 두 곳을 고칩니다. **둘 다 MIT라 고쳐서 배포해도
됩니다** — 저작권 표시와 라이선스 전문을 남기면 됩니다.

| 무엇 | 라이선스 | 왜 고쳤나 |
|---|---|---|
| `onnxruntime-react-native` | MIT | Gradle 9에서 제거된 API를 써서 빌드가 실패함 |
| `react-native-vision-camera` | MIT | 다섯 번에 한 번 카메라 목록이 비어 있음 |

**고친 파일을 통째로 올리지 않고 패치로 올립니다.** `nrh-test-app/patches/apply.mjs`가 설치 뒤에
자동으로 바릅니다. 그래야 원본이 무엇이고 우리가 무엇을 바꿨는지 한눈에 보입니다.

**두 고침 다 위쪽에 보낼 만한 것입니다.** vision-camera 쪽은 새 아키텍처에서 나는 경합
버그라 다른 사람도 겪습니다.

---

## 직접 짠 것

| 무엇 | 왜 직접 짰나 |
|---|---|
| JS 추적기(칼만·헝가리안) | Ultralytics BoT-SORT 구현을 TypeScript로 포팅 — **AGPL 파생물입니다** |
| JS 흔들림 보정 | opencv 없이 `sparseOptFlow`를 구현 |
| `.onnx` 메타데이터 리더 | onnxruntime의 JS API가 안 줘서 protobuf를 직접 읽음 |

**JS 모듈도 AGPL입니다.** 알고리즘만 참고한 게 아니라 벤더 코드를 보고 옮긴 것이라
파생물로 봐야 합니다.

---

## 공개할 때 챙길 것

- [ ] 세 저장소 모두에 `LICENSE`(AGPL-3.0 전문)를 넣는다
- [ ] 루트와 배포 패키지의 `LICENSE`, `NOTICE`를 유지한다
- [ ] Ultralytics 출처와 적용 버전을 배포 문서에 유지한다
- [ ] 웹 데모를 서버에 올린다면 **소스 받는 길**(저장소 링크)을 화면에 둔다
- [x] 학습 데이터의 이용 조건을 확인했다 — 아래 「학습 데이터」 절

**학습 데이터 조건은 아래 절에 있습니다.** 코드 라이선스와 별개입니다.

---

## 저장소에 든 영상

`nrh-model-js/demo/clips/sidewalk-640.mp4` (360×640, 20초, 2.2MB) 하나가 들어
있습니다. **직접 찍은 보행 영상**의 30~50초 구간이라 이 저장소와 같은 라이선스입니다.

**학습 데이터에서 나온 것이 아닙니다.** AI Hub 데이터로 만든 것(모델 가중치, 검증셋
사진)은 저장소에 안 들어갑니다 - 위 「공개할 때 챙길 것」을 볼 것.

---

## 학습 데이터 — AI Hub 「인도보행 영상」

> **이 결과물은 AI 허브의 「인도보행 영상」 데이터셋을 활용하였습니다.**

**이 문구를 지우지 마세요.** AI Hub가 2차 저작물 활용의 조건으로 요구한 것입니다
(2026-08-12 문의 답변).

### 되는 것

**학습된 모델 가중치(`.onnx`)는 공개·배포할 수 있습니다.** 영리·비영리 모두
가능합니다 — 원본 데이터를 포함하지 않는 2차 저작물이기 때문입니다.

### 안 되는 것

**AI Hub 원본 이미지·라벨은 어디에도 넣으면 안 됩니다.** 코드 동작 확인용이라도
안 됩니다. 저장소가 비공개여도 안 됩니다 — 접근 권한을 받은 사람이 열람할 수 있고,
**AI Hub에 직접 이용 신청하지 않은 사람은 제3자**이기 때문입니다.

**단순 편집·수정하거나 형식만 바꾼 것도 원본으로 봅니다.** 잘라내기, 크기 변경,
박스를 그린 것, 픽셀을 다른 형식으로 저장한 것 모두 해당합니다.

### 그래서 이 저장소가 하는 일

| 무엇 | 어디 |
|---|---|
| 모델 가중치 | 배포한다 (출처 표기와 함께) |
| 검증셋 사진 (`street-rgb.bin`) | `.gitignore`로 막고 **APK에도 안 넣는다** |
| 검증셋에서 생성한 기준값 | 원본 데이터에서 파생된 경우 함께 공개하지 않는다 |
| 데모 영상 | **직접 찍은 것**을 쓴다 |

**앱을 검증용으로 빌드할 때만** 사진이 들어갑니다
(`./gradlew assembleRelease -PwithFixture`). **그 APK는 남에게 주면 안 됩니다.**

### 보고서·발표 자료에도 마찬가지입니다

외부에 나가는 문서에 데이터셋 사진을 넣으면 안 됩니다. **직접 찍은 영상으로 그림을
만드세요.** 성능 수치·표·그래프는 2차 저작물이라 제약이 없습니다.
